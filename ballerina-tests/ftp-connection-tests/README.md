# FTP Connection Tests

Tests for `ballerina/ftp` client connection scenarios:

- Anonymous FTP connections
- Authenticated FTP connections
- Connection error cases (bad port, invalid hostname, wrong credentials)
- Connection lifecycle (close, operation-after-close)
