# FTP Test Commons

Shared constants and utilities for the `ballerina/ftp` module test suite.

Provides server connection details (host, ports, credentials) and local resource
file paths used across all `ballerina-tests/` packages.
